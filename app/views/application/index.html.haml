%section.hero.is-primary.is-bold.is-fullheight-with-navbar
  %div.hero-body
    %div.container.has-text-centered
      %h1.title World-class malware detection
      %h2.subtitle Get peace of mind with Checkr.
      = link_to 'Sign up today!', signup_path, { class: 'button is-medium is-link' }

%section.section
  %div.container.has-text-centered
    %p.subtitle Checkr works by comparing your files to a vast database of known malware.
    = image_tag('screenshot.png', { alt: 'Checkr Screenshot', style: 'width: 400px; max-width: 100%; border-radius: 16px; box-shadow: 1px 1px 5px lightgrey; margin: 32px 0;' })
    %p.subtitle Just upload any files you're suspicious of!

%section.section
  %div.container.has-text-centered
    %h1.title Convinced?
    = link_to 'Sign up today!', signup_path, { class: 'button is-large is-link' }
    %p.subtitle{ style: "margin-top: 8px;" } (It's free!)

%section.section
  %div.container.has-text-centered
    %p.subtitle.is-italic
      This application is not a real antivirus and provides no security.